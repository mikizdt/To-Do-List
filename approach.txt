1.HTML, create heading(h1), create form with input=text
and submit button -- this will be wrapped in a div#container so i
can target later in CSS to center and make child elements responsive.

2.CSS, center everything (using flexbox), add scrollbar for the 
task-container. 

3.JS.